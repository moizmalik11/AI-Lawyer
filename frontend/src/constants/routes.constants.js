/**
 * Application route constants.
 * Use these instead of magic strings across the app to prevent typos and make refactoring easier.
 */
export const ROUTES = {
    HOME: '/',
    AUTH: '/auth',
    DASHBOARD: '/dashboard',
    // We can add more routes here like:
    // SETTINGS: '/settings',
    // CONTRACTS: '/contracts',
};
